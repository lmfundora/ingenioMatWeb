# Convex
# Convex deployment name
CONVEX_DEPLOYMENT=
# Convex deployment URL (required)
VITE_CONVEX_URL=
# Site URL for Better Auth (auto-detected from VERCEL_URL in production)
SITE_URL=
# Convex site URL for client-side auth
VITE_CONVEX_SITE_URL=
